import logo from "@preview/assets/logo.png";

/**
 * Logo real do AcessoFast (cópia de public/favicon.png do app).
 * Importado como módulo para o Vite resolver a URL certa em dev e no build.
 */
export const LOGO_ACESSOFAST = logo;
